version: '3.3'

volumes:
  pg_data_production:
  static_volume_production:
  media_production:

services:
  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data_production:/var/lib/postgresql/data

  backend:
    image: nbkrtm/foodgram_backend
    env_file: .env
    volumes:
    - static_volume_production:/backend_static
    - media_production:app/media/recipes/images/
    depends_on:
    - db

  frontend:
    image: nbkrtm/foodgram_frontend
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - static_volume_production:/frontend_static

  gateway:
    image: nginx:1.19.3
    ports:
      - 8000:80
    volumes:
      - ../docs/:/usr/share/nginx/html/api/docs/
      - static_volume_production:/var/html/static/
      - media_production:/var/html/media/
    depends_on:
      - backend
